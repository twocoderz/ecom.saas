/**
 * Sort and result count bar.
 * JD mapping: top control bar on listing pages.
 */
export function SortBar() {
  return (
    <div className="rounded-xl border border-black/10 p-3">
      <p className="text-sm text-black/70">Tri + compteur de produits + vue grille/liste.</p>
    </div>
  )
}
